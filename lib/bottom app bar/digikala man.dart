import 'package:flutter/material.dart';

class digikalaman extends StatelessWidget {
  String name;
  String number;
  digikalaman({Key? key, this.name = '', this.number = ''}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(),
    );
  }
}
